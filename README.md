# User Level Threads Implementation in C
A basic implementation of user level threads in C as part of the course, Distributed Operating Systems

# To Run
`gcc tcb.h && gcc q.h && gcc thread.h && gcc thread.c && ./a.out`
